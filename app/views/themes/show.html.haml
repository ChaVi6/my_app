%p{style: "color: green"}= notice

= render @theme

%div
  = link_to t('.edit_theme'), edit_theme_path(@theme)
  |
  = link_to t('.back_to_themes'), themes_path
  |
  = button_to t('.destroy_theme'), @theme, method: :delete