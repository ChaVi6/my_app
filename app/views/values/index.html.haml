%p{style: "color: green"}= notice

%h1= t('.values')

%div{id: "values"}
  - @values.each do |value|
    = render value
    %p
      = link_to t('.show_value'), value

= link_to t('.new_value'), new_value_path