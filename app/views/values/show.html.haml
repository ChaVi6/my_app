%p{style: "color: green"}= notice

= render @value

%div
  = link_to t('.edit_value'), edit_value_path(@value)
  |
  = link_to t('.back_to_values'), values_path
  |
  = button_to t('.destroy_value'), @value, method: :delete