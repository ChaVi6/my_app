.form_select
  = form_tag(display_theme_path, remote: true, method: :post) do
    = select_tag :theme, options_for_select(@themes, selected: @selected_theme), prompt: t('.select_theme_prompt'), onchange: "$(this).parent('form').submit();", style: "width: 200px;"