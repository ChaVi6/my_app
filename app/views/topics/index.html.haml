%p{style: "color: green"}= notice

%h1= t('.topics')

%div{id: "topics"}
  - @topics.each do |topic|
    = render topic
    %p
      = link_to t('.show_topic'), topic

= link_to t('.new_topic'), new_topic_path