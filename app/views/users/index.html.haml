%p{style: "color: green"}= notice

%h1= t('.users')

%div{id: "users"}
  - @users.each do |user|
    = render user
    %p
      = link_to t('.show_user'), user

= link_to t('.new_user'), new_user_path